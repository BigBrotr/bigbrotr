# ============================================================================
# BigBrotr Implementation Template - Environment Configuration
# ============================================================================
# Copy this file to .env and configure your settings
# Command: cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# DATABASE PASSWORD (REQUIRED)
# ----------------------------------------------------------------------------
# Password for PostgreSQL database connection.
# Used by all services to connect to the database.
#
# Generate a secure password:
#   openssl rand -base64 32
#
# IMPORTANT: Keep this secret and never commit .env to version control
# ----------------------------------------------------------------------------

DB_PASSWORD=

# ----------------------------------------------------------------------------
# PRIVATE KEY (OPTIONAL but recommended)
# ----------------------------------------------------------------------------
# Nostr private key (hex format, 64 characters)
# Used by services for various operations:
#
# Monitor service:
#   - Test relay write capability (publish test events)
#   - Verify relay read capability (query test events)
#   - Publish NIP-66 relay discovery events
#
# Validator service:
#   - NIP-42 authentication for auth-required relays
#
# Synchronizer service:
#   - NIP-42 authentication for auth-required relays
#
# Without this key:
#   - Monitor will skip write tests
#   - Validator/Synchronizer won't authenticate to auth-required relays
#
# Generate a new key:
#   openssl rand -hex 32
#
# Or use existing key from your Nostr client (export in hex format)
# ----------------------------------------------------------------------------

PRIVATE_KEY=

# ----------------------------------------------------------------------------
# GRAFANA PASSWORD (OPTIONAL)
# ----------------------------------------------------------------------------
# Password for Grafana admin user. Default: admin
# GRAFANA_PASSWORD=admin

# ----------------------------------------------------------------------------
# OPTIONAL SETTINGS
# ----------------------------------------------------------------------------
# Uncomment and modify as needed

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# Environment: development, production
# ENVIRONMENT=production
