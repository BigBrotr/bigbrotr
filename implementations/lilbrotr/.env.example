# ============================================================================
# LilBrotr Environment Configuration
# ============================================================================
# Copy this file to .env and configure your settings
# Command: cp .env.example .env
#
# Note: LilBrotr is a lightweight implementation that does not store tags/content
# ============================================================================

# ----------------------------------------------------------------------------
# DATABASE PASSWORD (REQUIRED)
# ----------------------------------------------------------------------------
# Password for PostgreSQL database connection.
# Used by all services to connect to the database.
# ----------------------------------------------------------------------------

DB_PASSWORD=

# ----------------------------------------------------------------------------
# PRIVATE KEY (REQUIRED for full NIP-66 testing and NIP-42 authentication)
# ----------------------------------------------------------------------------
# Nostr private key (hex, 64 characters) used by all services:
#
# Monitor service:
#   - Test relay write capability (publish test events)
#   - Verify relay read capability (query test events)
#   - Measure round-trip time for operations
#   - Publish NIP-66 relay discovery events
#
# Validator service:
#   - NIP-42 authentication for auth-required relays
#
# Synchronizer service:
#   - NIP-42 authentication for auth-required relays
#
# Without this key:
#   - Monitor will only fetch NIP-11 info and skip write tests
#   - Validator/Synchronizer won't authenticate to auth-required relays
#
# Generate a new key: openssl rand -hex 32
# ----------------------------------------------------------------------------

PRIVATE_KEY=
