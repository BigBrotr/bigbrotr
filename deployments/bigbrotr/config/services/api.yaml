pool:
  user: bigbrotr_reader
  password_env: DB_READER_PASSWORD
  min_size: 2
  max_size: 5

interval: 60.0
host: "0.0.0.0"
port: 8080
max_page_size: 1000
default_page_size: 100

tables:
  service_state:
    enabled: false

metrics:
  enabled: true
  host: "0.0.0.0"
