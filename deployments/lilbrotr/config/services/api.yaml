# =============================================================================
# API Service Configuration (LilBrotr)
# =============================================================================
# Read-only HTTP API for querying relay and event data.
# Only values that differ from defaults are specified here.
# =============================================================================

pool:
  user: lilbrotr_reader
  password_env: DB_READER_PASSWORD
  min_size: 2

interval: 60.0

tables:
  service_state:
    enabled: false
  pg_stat_statements:
    enabled: false
  pg_stat_statements_info:
    enabled: false

metrics:
  enabled: true
  host: "0.0.0.0"
