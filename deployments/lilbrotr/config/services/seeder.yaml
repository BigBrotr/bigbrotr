# =============================================================================
# Seeder Service Configuration (LilBrotr)
# =============================================================================
# One-shot service that loads initial relay URLs from a seed file.
# This deployment file keeps deployment-local overrides and a few restated
# operator-facing defaults. See docs/user-guide/services.md and
# src/bigbrotr/services/seeder/configs.py for the full config contract.
# =============================================================================

pool:
  user: writer
  password_env: DB_WRITER_PASSWORD
  max_size: 2
