# =============================================================================
# Seeder Service Configuration (LilBrotr)
# =============================================================================
# Only values that differ from defaults are specified here.
# See _template/config/services/seeder.yaml for all options and documentation.
# =============================================================================

# All values use defaults - seed file path and to_validate behavior
