# =============================================================================
# Seeder Service Configuration (LilBrotr)
# =============================================================================
# One-shot service that loads initial relay URLs from a seed file.
# Only values that differ from defaults are specified here.
# See brotr/config/services/seeder.yaml for all options and documentation.
# =============================================================================

pool:
  user: lilbrotr_writer
  password_env: DB_WRITER_PASSWORD
  max_size: 2
