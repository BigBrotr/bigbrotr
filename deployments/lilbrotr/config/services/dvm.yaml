pool:
  user: lilbrotr_reader
  password_env: DB_READER_PASSWORD
  min_size: 1
  max_size: 3

interval: 60.0
relays:
  - "wss://relay.damus.io"
  - "wss://nos.lol"
  - "wss://relay.nostr.band"
kind: 5050
max_page_size: 1000
announce: true

tables:
  service_state:
    enabled: false

metrics:
  enabled: true
  host: "0.0.0.0"
