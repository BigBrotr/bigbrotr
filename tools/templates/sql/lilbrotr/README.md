# sql/lilbrotr

Lightweight-profile SQL overrides.

## What Lives Here

- the template overrides that adapt the shared SQL contract to the
  `lightweight_archive` storage profile.

## Rules

- Keep overrides minimal and explicit relative to `sql/base/`.
