# sql/base

Shared SQL templates used by all built-in storage profiles.

## What Lives Here

- core schema, derived tables, CRUD functions, refresh functions, indexes, and
  verification templates that define the common SQL contract.

## Rules

- Change shared schema and shared procedure behavior here first.
