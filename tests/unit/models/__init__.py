"""Unit tests for the pure shared ``bigbrotr.models`` data contract.

This package covers model invariants such as relay-URL validation, enum
behavior, content-addressed documents, archived events, observations, and
shared service-state records. The suite stays zero-I/O so model constructors
remain the only validation boundary under test.
"""
