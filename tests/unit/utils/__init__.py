"""Unit tests for low-level ``bigbrotr.utils`` helpers and protocol seams.

Coverage here stays focused on DNS, HTTP, key loading, transport, protocol
facades and submodules, plus bounded event-stream helpers consumed by higher
layers.
"""
