# unit/nips

Unit tests for protocol-aware NIP helpers and builders.

## What Lives Here

- top-level NIP package tests plus subfolders for `nip11`, `nip66`, and
  `nip85`.

## Rules

- Keep protocol semantics explicit and localized to the NIP layer.
