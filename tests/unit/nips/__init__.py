"""Unit tests for shared ``bigbrotr.nips`` package surfaces and seams.

This suite covers the top-level NIP registry, shared base classes, parse/report
helpers, and event builders that sit above the concrete ``nip11``, ``nip66``,
and ``nip85`` subpackages.
"""
